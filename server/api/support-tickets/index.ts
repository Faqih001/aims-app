import { db } from '~/server/utils/db'; import { support-tickets } from '~/server/db/schema'; import { createCrudHandlers } from '~/server/utils/crudTools'; const crud = createCrudHandlers(support-tickets); export default defineEventHandler(async (event) => { if(event.method === 'GET') return await crud.getAll(db); if(event.method === 'POST') { const body = await readBody(event); return await crud.create(db, body); } });
